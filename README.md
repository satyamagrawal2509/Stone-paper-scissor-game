# 🎮 Python Game (Random Module + File Handling)
# Stone-paper-scissor-game
a simple stone paper scissor game using random module

  
The game uses:
- Python's built-in `random` module  
- File handling (reads/writes data to `data.txt`)

This project is perfect for beginners learning Python basics, logic, and file operations.

---

## 🧩 Features
- Entire game in **one Python file**
- Uses random number generation
- Saves/reads data using a `.txt` file
- Beginner-friendly and easy to understand

---

## 🛠 Requirements
You only need:

- **Python 3.7+**

No external libraries needed.  
(Only Python’s built-in `random` module is used.)

